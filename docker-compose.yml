version: "3.9"

services:
  backend:
    build: .
    container_name: hello-backend
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=uat
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: hello-redis